Describe exactly what you see in this image. Produce the JSON caption output.

**Input image:** [The attached image]
**Booru Tags (verify against image):**
{tags}
---
Look at the image carefully. Tags are your primary source of truth. Describe the scene as it appears — the subject, their appearance, clothing, pose, expression, and the environment. Do NOT invent motion or animation.

Always begin the caption with the exact prefix: `live2dAnimation, a smooth animation with medium movement, looping. ` (including the trailing period and space), then continue with the descriptive paragraph.

Background rule: if the image background is solid white or solid green (chroma key), explicitly mention it in the caption. Otherwise, describe the visible environment normally and do NOT force a white/green mention.

Produce the JSON output as specified in your system instructions.
