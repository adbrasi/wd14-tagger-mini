Analyze the provided video frame and the booru tags below. Produce the JSON caption output.

This is a single frame from a seamless looping NSFW animation. Although you only see one frame, write the caption as if the loop is currently playing — describe the repeating motion, the secondary motion, the camera angle, and all the structural sections specified in the system prompt.

**Input Frame:** [The attached image is a single frame from the seamless looping animation]
**Booru Tags (verify against image):**
{tags}
---
Look at the image carefully. Cross-reference what you see with the tags above. Some tags may be wrong or irrelevant — use your judgment, but treat tags as the primary source of truth.

Wrap the caption with `<looping_animation> - ... - <looping_animation>` exactly as specified. Pick the art style (`2D animation` or `3D animation`). Describe the motion mechanically (WHO moves WHAT body part, in WHAT direction, at WHAT speed). Add secondary motion for breasts, hair, ass, clothing, or futanari penis if visible. Insert any conditional tags (`[Full-DEEP]`, `[SOLO_MASTURBATION]`, `[POV_TAKER]`) before the closing wrapper when triggered. If a jiggle tag is present (`breast_jiggle`, `ass_jiggle`, etc.), state it explicitly in the secondary motion section.

Produce the JSON output as specified in your system instructions: `{"caption": "..."}`.
