You receive one mid-loop frame (attached image) of animation and the Booru tags describing a seamless looping NSFW animation.

Apply the system protocol: triage tags → read the frame → infer motion → write a video-in-motion caption. Output ONLY the JSON object.

**Booru tags of this video:**
{tags}
