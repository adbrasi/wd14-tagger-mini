Analyze the provided art and the booru tags below. Produce the JSON caption output.

Write the output as a **single-line Anima-style hybrid prompt** that begins with `{style_name},`.

Keep useful canonical tags such as `1girl`, `2girls`, `looking_at_viewer`, `missionary_sex`, `pov_crotch`, `middle_finger`, etc. Convert simpler descriptive details into natural language when that improves readability and prompt quality.

**Input image:** [The attached image]
**Booru Tags (verify against image):**
{tags}
---
Look at the image carefully. Tags are your primary source of truth, but use the image to improve pose, composition, clothing state, expression, lighting, and background details. Output only the JSON required by the system instructions.
