Analyze the provided art and the booru tags below. Produce the JSON caption output.

The caption must begin with `{character_name},` and then continue as natural language.

**Input image:** [The attached image]
**Booru Tags (verify against image):**
{tags}
---
Look at the image carefully. Use the tags as your primary source of truth, but use the image to refine pose, colors, expression, clothing details, and scene layout. Produce the JSON output exactly as required by the system instructions.
